{
  "name": "bookmarklet-of-destiny",
  "version": "1.0.0",
  "private": true,
  "description": "A self-contained terminal dashboard bookmarklet.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "qrcode-generator": "^2.0.4"
  },
  "devDependencies": {
    "esbuild": "^0.25.5",
    "playwright": "^1.58.2"
  }
}
